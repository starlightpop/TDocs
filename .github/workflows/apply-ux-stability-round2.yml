name: Apply UX stability round 2

on:
  push:
    branches:
      - agent/tdocs-word-docs-foundation
    paths:
      - scripts/apply-ux-stability-round2.py
      - scripts/repair-ux-round2-runner.py
      - .github/workflows/apply-ux-stability-round2.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/tdocs-word-docs-foundation
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Harden refactor boundaries
        run: python3 scripts/repair-ux-round2-runner.py
      - name: Apply UX stability refactor
        run: python3 scripts/apply-ux-stability-round2.py
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
      - name: Build renderer
        run: npm run build
      - name: Commit verified fixes
        run: |
          git config user.name "TDocs Refactor Bot"
          git config user.email "actions@users.noreply.github.com"
          git add src tests
          git commit -m "fix: stabilize code blocks, overlays and pagination"
          git push origin HEAD:agent/tdocs-word-docs-foundation
