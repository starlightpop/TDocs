# TDocs 0.2.0-preview

这是一次产品边界收缩与稳定性重构。该预览版专注于本地优先的连续文档编辑。

## 主要变化

- 无限文档改为动态增长：保留左右边界，并在底部进入视口前继续延长；末尾标题也能跳到编辑区顶部。
- 修复代码块旧绝对定位样式覆盖新布局的问题，恢复正常输入位置、行号对齐和横向滚动。
- 运行嵌入代码片段时移除公共外层缩进，避免单行 Python 因文档缩进误报 `unexpected indent`。
- 修复无限文档高度回退：保留文档边界，同时让空文档始终比当前视口更长，并随内容自然延伸。
- 重构代码块 DOM 与行号布局，恢复正常换行、缩进、选择、补全和横向滚动。
- 完整移除未达到发布标准的 Word/A4/B5 工作区、分页节点、分页符和内容切分逻辑。
- 所有旧文件统一迁移为连续文档，正文内容与文件夹信息保留。
- 设置中心精简为常规、外观和 AI 模型三类。
- 新建入口改为单一、完整的“新建文档”按钮。
- AI 厂商与模型下拉菜单互斥显示，并限制浮层高度和滚动范围。
- 恢复中文完整右键菜单：撤销、重做、剪切、复制、粘贴、匹配样式粘贴、删除、全选，以及正文格式与 AI 操作。
- 代码块统一行号与正文的字体、行高和内边距；语言选择器可直接点击；保留当前行高亮、补全和块内运行。
- 代码运行继续调用用户电脑已有环境；环境缺失时显示原因、官方安装地址和对应系统命令。
- macOS 顶部空白区域双击最大化改由 Electron 主进程处理。
- 保留 AI 改写、查找替换、本地版本历史、文件夹、置顶、大纲和多格式导出。

## 版本线说明

此前基于 0.1.x 的开发迭代归入 `0.1.2-preview`。本次清除错误分页架构并重新确定产品边界后，发布 `0.2.0-preview`。

## 已知限制

- 这是预发行版本，尚未进行 Apple Developer 签名和公证；macOS 首次打开时可能需要在系统安全设置中确认。
- Python、C/C++、Java、Rust、MATLAB/Octave 等运行环境不会随应用打包，需要用户自行安装。
- 当前依赖树仍存在上游构建链安全告警，未使用破坏性的强制升级处理。
