# TDocs 0.2.1-preview

本次发布主要聚焦 AI 改写体验升级：流式输出、可中断、多选区批量和双栏差异预览。

## 主要变化

### AI 改写：流式 + 可中断

- 改写请求不再等待完整响应，首次 token 平均 200~600ms 内到达终端用户可见。
- UI 增加“正在改写…（已收到 N tokens）”状态、停止按钮，键盘上 Esc / ⌘. 也能中止。
- `AbortController` 在 Vue 这边是上下游共用的；用户中止后中间结果会清空，不在文档里留草稿。
- 5xx 或网络层错误重试 2 次（指数退避 800ms / 1.6s）；4xx 直接报错，不重试。
- 连接 15s 没有首个 chunk 视为连接超时；首次 chunk 后超过 60s 没有新数据视为整体超时。
- SSE `text/event-stream` 解析提取 `choices[0].delta.content`，同时兼容 `choices[0].text` 兑底。

### 多选区批量改写

- BubbleBar 上的多选区开关开启后，用户按 ⌘/Ctrl 多选多处，点 AI 一次性发起合并请求。
- 输入端：prompt 以 `<!--TDOCS_SPLIT n-->` 标记段号。
- 输出端：纯字符串拆分回填到对应 range；任何一段失败都兑底为原文并提示「第 N 段改写失败」。

### 双栏差异预览重构

- 左侧表上单选模式下输出变为 “原文 / 改写”并排，每段以 word-level LCS 拆出 `eq / add / del`。
- “接受差异”动作只插入 add + eq，不回填 del。
- “全部接受” / “全部撤销” / “全部替换”仍然是独立动作，不会自动覆盖彼此。

### 提示词模板持久化

- 5 条内建改写 + 用户最近 12 条自定义 prompt 持久化到 IndexedDB（key `tdocs.ai.templates`）。
- IDB 不可用时降级到 localStorage（同一个 JSON schema）。
- 「最近使用」折叠面板以中最近 12 条。

## 已知限制

- 这是预发行版本，尚未进行 Apple Developer 签名和公证；macOS 首次打开时可能需要在系统安全设置中确认。
- 某些国产模型 API 在 SSE 回复上与现代 OpenAI 兼容不一致（有时流错或是返回 schema 不一样）。后续在设置中加一个「流式」开关：关闭后走经典 POST + JSON 路径，不动现有调用点。
- “接受差异”只写入 add + eq，不会从文档中删除先前插入的“原文”部分。所以“从单选改为多选”多批里看会出现原文与改写同名重复的字样，请用“全部替换”或“全部撤销”统一清理。
- 部分会话 token 计数是近似估算（中文按字、英文按词），不作为计费依据。

## 依赖与构建

- 增量依赖：未引入新包。LCS / SSE 都是手写实现，`tests/*` 仍以 `node:test` 运行。
- 测试：现共 86 个，含本版本新增 3 组计 26 条。
