# 更新记录

## 0.2.1-preview

- 新增 `/` 命令面板（飞书云文档同款）：在段落或标题中输入 `/` 弹出浮层菜单，键盘 ↑/↓/Enter 选中，可插入正文/标题/列表/引用/代码块/分割线/表格/图片/链接，也可一键触发 AI 润色、精简、扩写、正式、口语，或打开查找、版本历史、导出。支持关键词模糊匹配，中英文同义词都能命中。代码块内不触发。
- 新增 ⌘P / ⌘K 快速跳转：跨文档搜索标题与正文，附带高亮片段和最近打开；选中后切换文档并定位到匹配位置。拦截浏览器 ⌘P 打印默认行为。
- 存储迁移到 IndexedDB：完整文档、版本历史、恢复快照和图片 blob 全部进入 `tdocs.v1`；元数据（活动文档、主题、分组等）进入 `meta` store。首次启动自动从 localStorage 迁移并写入 `__idb_migrated__` 标记。
- 引入崩溃恢复：切换文档前与编辑过程中将内容写入 `recovery` store，启动时检测最近 7 天内未清理的快照，弹"恢复未保存内容？"面板。
- 保存状态由假定时器改为真实写入回调：状态栏显示 `已保存 / 保存中… / 保存失败`，写入失败时保留旧版本并提供重试入口。
- 文档体积软阈值：超过 200KB 出现黄色提示 chip；超过 500KB 出现红色"建议拆分或导出"提示。
- 图片改存图片 store：拖入 / 粘贴 >200KB 的图片自动压缩为 WebP（1600px、quality 0.82），正文只保留 `<img data-tdocs-img="...">` + `tdocs://img/` 占位；导出前自动把图片回填为 base64。状态栏提供"压缩图片"手动入口。
- AI 改写接入流式输出（`POST /chat/completions?stream=true`，SSE 增量解析），改写过程中显示“正在改写…（已收到 N tokens）”并可随时中断；设置 → AI 模型提供 “流式输出” 开关，可在不兼容 SSE 的厂商上回退为一次性 POST + JSON。
- 增加停止按钮 + Esc / ⌘. 中断快捷键；接到 AbortController 后清空中间结果，不残留重。
- 网络错误或 5xx 走指数退避重试（默认 2 次），4xx 不重试并立即报错；连接超时 15s、整体超时 60s。
- 新增多选区批量 AI 改写：BubbleBar 上的多选区开关 + ⌘/Ctrl 多选 + 一次请求合并提交，返回按 `<!--TDOCS_SPLIT n-->` 拆回、缺失段回退为原内容并提示“第 N 段改写失败”。
- 双栏差异预览重构：以 word-level LCS 为基础，在同一行并排呈现“原文 / 改写”两栏（新增绿色下划线、删除红色删除线），提供“接受差异”（仅写入 add）、“全部接受”、“全部撤销”、以及旧版的“全部替换”。
- 提示词模板持久化：5 条内建改写提示 + 用户最近 12 条自定义 prompt 持久化到 IndexedDB（key `tdocs.ai.templates`），IDB 不可用时降级到 localStorage，下次自动出现在”最近使用“面板。
- 补充 3 组单测（`diff.test.mjs` / `streamParser.test.mjs` / `multiSelectionSplit.test.mjs`）：不发起 HTTP，覆盖 LCS 边界、SSE 增量与多选区拆分/兜底。

## 0.2.0-preview

- 删除未达到发布标准的 Word/A4/B5 工作区、分页节点和内容切分逻辑。
- 产品重新聚焦本地优先的连续文档编辑。
- 统一设置中心、菜单层级、新建入口、代码块行号和语言选择控件。
- 恢复中文完整右键菜单，稳定 macOS 标题栏双击最大化。
- 保留 AI 改写、查找替换、版本历史、代码运行及多格式导出。

## 0.1.2-preview

- 该版本号用于归档此前基于 0.1.x 的开发迭代。
- 包含 AI 选区高亮、多厂商模型配置、查找替换、本地版本历史和代码运行基础。
- 其中的分页试验未达到发布标准，未作为 0.2.0 功能保留。
